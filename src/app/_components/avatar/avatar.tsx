import { classNames } from '@/util/string.util'

const Avatar = () => {
  return <div className={classNames("w-10", "aspect-square", "bg-blue-300", "rounded-full")}>

  </div>
}

export default Avatar